{
  "name": "nuancier",
  "private": true,
  "version": "0.0.1",
  "description": "Atelier de couleur pour graphistes — palettes fonctionnelles, lisibles, responsables",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json && tsc -p tsconfig.server.json",
    "build:back": "esbuild back/standalone.ts --bundle --format=esm --outfile=back.js --banner:js=\"// Généré par 'npm run build:back' — ne pas éditer à la main (source : back/standalone.ts + back/api.ts). BACK de Nuancier : à coller dans un Worker créé depuis le template Hello World.\"",
    "build:front": "vite build && node scripts/build-front-file.mjs",
    "build:colle": "npm run build:front && npm run build:back",
    "dev:full": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "apca-w3": "0.1.9",
    "culori": "^4.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/culori": "^2.1.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.118.0"
  }
}
