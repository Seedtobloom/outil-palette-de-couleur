{
  "name": "nuancier",
  "private": true,
  "version": "0.0.1",
  "description": "Atelier de couleur pour graphistes — palettes fonctionnelles, lisibles, responsables",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json && tsc -p tsconfig.server.json",
    "build:worker": "esbuild worker/standalone.ts --bundle --format=esm --outfile=worker-dashboard.js --banner:js=\"// Généré par 'npm run build:worker' — ne pas éditer à la main (source : worker/standalone.ts + shared/api.ts).\"",
    "dev:full": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "apca-w3": "0.1.9",
    "culori": "^4.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/culori": "^2.1.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.118.0"
  }
}
